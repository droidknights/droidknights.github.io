import assert from 'assert';

export default {
  inserted(el, binding, vnode) {
    assert.strictEquals(el.tagName, 'INPUT');

    this.$autoCompleter = new google.maps.places.Autocomplete(el);
    
  }
}
